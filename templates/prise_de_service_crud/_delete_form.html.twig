<form method="post" action="{{ path('app_prise_de_service_crud_delete', {'id': prise_de_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prise_de_service.id) }}">
    <button class="btn">Delete</button>
</form>
