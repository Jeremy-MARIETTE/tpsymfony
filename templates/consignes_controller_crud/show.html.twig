{% extends 'base.html.twig' %}

{% block title %}Consignes{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Consignes</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ consigne.site.nom }}</td>
                </tr>
                <tr>
                    <th>Content</th>
                    <td>{{ consigne.content }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-primary" href="{{ path('app_consignes_controller_crud_index') }}">Retour à la liste</a>

        <a class="btn btn-primary" href="{{ path('app_consignes_controller_crud_edit', {'id': consigne.id}) }}">Modifier</a>

        {{ include('consignes_controller_crud/_delete_form.html.twig') }}
        </div>
{% endblock %}
