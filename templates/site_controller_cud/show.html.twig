{% extends 'base.html.twig' %}

{% block title %}Site{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Site</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ site.nom }}</td>
                </tr>
                 <tr>
                    <th>Adresse</th>
                    <td></td>
                </tr>
                 <tr>
                    <th>Ville</th>
                    <td></td>
                </tr>
                <tr>
                    <th>Cp</th>
                    <td>{{ site.cp }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-primary" href="{{ path('app_site_controller_cud_index') }}">Retour</a>

        <a class="btn btn-primary" href="{{ path('app_site_controller_cud_edit', {'id': site.id}) }}">Editer</a>

        {{ include('site_controller_cud/_delete_form.html.twig') }}
    </div>
{% endblock %}
