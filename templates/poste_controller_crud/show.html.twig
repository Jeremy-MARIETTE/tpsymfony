{% extends 'base.html.twig' %}

{% block title %}Poste{% endblock %}

{% block body %}
    <div class="container">
        <h1>Poste</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Site</th>

                    <tr>
                    <td>{{ poste.agent.nom }}</td>
                    <td>{{ poste.agent.prenom }}</td>
                    <td>{{ poste.site.nom }}</td>
                    </tr>
                    
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_poste_controller_crud_index') }}">back to list</a>

        <a href="{{ path('app_poste_controller_crud_edit', {'id': poste.id}) }}">edit</a>

        {{ include('poste_controller_crud/_delete_form.html.twig') }}
    </div>
{% endblock %}
