{% extends 'base.html.twig' %}

{% block title %}SiteUser{% endblock %}

{% block body %}
    <div class="container">
        <h1>SiteUser</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ site_user.id }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_site_user_controller_crud_index') }}">back to list</a>

        <a href="{{ path('app_site_user_controller_crud_edit', {'id': site_user.id}) }}">edit</a>

        {{ include('site_user_controller_crud/_delete_form.html.twig') }}
    </div>
{% endblock %}
