{% extends 'base.html.twig' %}

{% block title %}Modifier mot de passe{% endblock %}

{% block body %}

    <div class="container">
        <h1 class="text-center">Modifier votre mot de passe</h1>

        {% for flashMessage in app.flashes('error') %}
            <div class="alert alert-danger">
                {{ flashMessage }}
            </div>
        {% endfor %}
         {% for flashMessage in app.flashes('success') %}
            <div class="alert alert-success">
                {{ flashMessage }}
            </div>
        {% endfor %}

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="col btn btn-success" type="submit">Mise à jour</button>
        {{ form_end(form) }}
    </div>

{% endblock %}
